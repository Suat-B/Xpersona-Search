# Copy to .env.local and fill in values. Never commit .env.local.

# Required for NextAuth (Google sign-in, session, guest & agent tokens)
NEXTAUTH_SECRET=your-32-char-or-longer-secret-here
NEXTAUTH_URL=http://localhost:3000

# Multi-service subdomains (production): xpersona.co=hub, game.xpersona.co, trading.xpersona.co
# For local dev: add to hosts "127.0.0.1 game.localhost trading.localhost"
# Or use ?service=game or ?service=trading on localhost
# NEXT_PUBLIC_APP_URL=https://xpersona.co

# Required for Google sign-in
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Required for database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# Required for Stripe (checkout + webhook)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Webhook events for ANS: checkout.session.completed, invoice.payment_succeeded, customer.subscription.deleted

# Optional: credit package Price IDs (for npm run seed)
# STRIPE_PRICE_500=
# STRIPE_PRICE_2000=
# STRIPE_PRICE_10000=

# Vercel Cron: CRON_SECRET sent as Bearer token when invoking cron jobs (e.g. /api/cron/ans-verify)
# CRON_SECRET=generate-with-openssl-rand-hex-32

# ANS (Agent Name Service) — .xpersona.agent domains
# Required for full registration: STRIPE_PRICE_ID_ANS_STANDARD, MASTER_ENCRYPTION_KEY
# Pro tier optional (for future tier selection in UI)
# STRIPE_PRICE_ID_ANS_STANDARD=price_xxx
# STRIPE_PRICE_ID_ANS_PRO=price_xxx
# MASTER_ENCRYPTION_KEY=64-char-hex-for-aes-256  # node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Upstash Redis (optional; for production rate limiting across serverless instances)
# If unset, in-memory rate limiting is used (single-instance only)
# UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=
# Cloudflare DNS (optional; if missing, registration still works, DNS instructions shown)
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ZONE_ID=
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_ORIGIN_IP=76.76.21.21
# ANS_DOMAIN=xpersona.agent
# ROOT_DOMAIN=xpersona.co

# Optional: app constants (defaults in code)
# SIGNUP_BONUS=100
# FAUCET_AMOUNT=50
# MIN_BET=1
# MAX_BET=10000

# Admin panel: comma-separated emails that can access /admin
# ADMIN_EMAILS=you@example.com,other@example.com

# Password reset email (Resend). Without this, reset links are logged to console in dev.
# RESEND_API_KEY=re_xxx
# EMAIL_FROM="Xpersona <noreply@xpersona.co>"
