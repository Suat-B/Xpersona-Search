export type OpenHandsRuntimeKind = "docker" | "local-python" | "remote" | "reduced-local" | "unknown";
export type OpenHandsRuntimeProfile = "full" | "code-only" | "chat-only" | "unavailable";
export type OpenHandsRuntimeReadiness = "ready" | "limited" | "repair_needed";
type GatewayHealthDoctor = {
    ok?: boolean;
    runtimeKind?: OpenHandsRuntimeKind;
    runtimeProfile?: OpenHandsRuntimeProfile;
    pythonVersion?: string | null;
    packageFamily?: "openhands" | "openhands-sdk" | "unknown";
    packageVersion?: string | null;
    supportedTools?: string[];
    degradedReasons?: string[];
    availableActions?: string[];
    currentModelCandidate?: Record<string, unknown> | null;
    lastProviderFailureReason?: string | null;
    fallbackAvailable?: boolean;
    lastFallbackRecovered?: boolean;
    lastPersistenceDir?: string | null;
};
type GatewayHealthPayload = {
    status?: "healthy" | "degraded" | "unhealthy";
    title?: string;
    runtime?: string;
    version?: string | null;
    gatewayUrl?: string;
    doctor?: GatewayHealthDoctor;
    message?: string;
    details?: string;
};
type PersistedRuntimeState = {
    selectedRuntimeKind?: OpenHandsRuntimeKind;
    selectedRuntimeProfile?: OpenHandsRuntimeProfile;
    selectedAt?: string;
    lastHealthyAt?: string;
    gatewayUrl?: string;
    failureCount?: number;
    nextRetryAt?: string;
    lastMessage?: string;
    lastDegradedReasons?: string[];
};
export type OpenHandsRuntimeStatus = {
    readiness: OpenHandsRuntimeReadiness;
    runtimeKind: OpenHandsRuntimeKind;
    runtimeProfile: OpenHandsRuntimeProfile;
    gatewayUrl: string;
    version?: string | null;
    pythonVersion?: string | null;
    packageFamily?: "openhands" | "openhands-sdk" | "unknown";
    packageVersion?: string | null;
    supportedTools: string[];
    degradedReasons: string[];
    availableActions: string[];
    message: string;
    selectedAt?: string;
    lastHealthyAt?: string;
    currentModelCandidate?: Record<string, unknown> | null;
    lastProviderFailureReason?: string | null;
    fallbackAvailable?: boolean;
    lastFallbackRecovered?: boolean;
    lastPersistenceDir?: string | null;
};
type EnsureRuntimeInput = {
    desiredProfile: OpenHandsRuntimeProfile;
};
type SupervisorOptions = {
    statePath: string;
    repoRoot: string;
    gatewayUrl?: string;
};
export declare function parseLocalGatewayBinding(gatewayUrl: string): {
    host: string;
    port: string;
};
export declare function needsManagedOpenHandsInstall(executableExists: boolean, sdkImportable: boolean): boolean;
export declare function inferReadinessFromProfile(actual: OpenHandsRuntimeProfile, desired: OpenHandsRuntimeProfile): OpenHandsRuntimeReadiness;
export declare function mapGatewayHealthToOpenHandsRuntimeStatus(input: {
    gatewayUrl: string;
    payload?: GatewayHealthPayload | null;
    persisted?: PersistedRuntimeState;
    desiredProfile: OpenHandsRuntimeProfile;
    fallbackReasons?: string[];
}): OpenHandsRuntimeStatus;
export declare function inferOpenHandsRuntimeProfile(task: string): OpenHandsRuntimeProfile;
export declare function getPreferredOpenHandsRuntimeKinds(platform: NodeJS.Platform, desiredProfile: OpenHandsRuntimeProfile): OpenHandsRuntimeKind[];
export declare class OpenHandsRuntimeSupervisor {
    private readonly statePath;
    private readonly repoRoot;
    private readonly gatewayUrl;
    private readonly requirementsPath;
    private readonly managedRuntimeRoot;
    private readonly managedVenvDir;
    private state;
    private lastHealthPayload;
    private lastHealthAt;
    private warmupPromise;
    constructor(options: SupervisorOptions);
    initialize(): Promise<void>;
    getStatus(desiredProfile?: OpenHandsRuntimeProfile): Promise<OpenHandsRuntimeStatus>;
    ensureRuntime(input: EnsureRuntimeInput): Promise<OpenHandsRuntimeStatus>;
    warmup(desiredProfile?: OpenHandsRuntimeProfile): Promise<OpenHandsRuntimeStatus>;
    private isCoolingDown;
    private inspectGatewayHealth;
    private recordHealthy;
    private recordFailure;
    private persist;
    private tryStartLocalPythonRuntime;
    private tryStartDockerRuntime;
    private waitForHealthyGateway;
    private getPythonCandidates;
    private getBootstrapPythonCandidates;
    private getManagedPythonExecutable;
    private ensureManagedPythonEnv;
    private canImportOpenHands;
    private stopLocalGatewayProcesses;
    private checkCommand;
    private spawnDetached;
    private runCommand;
}
export {};
